version: '3.8'

services:
  bot:
    image: "${BOT_IMAGE_NAME:-tg_bot-image}"
    container_name: "${BOT_CONTAINER_NAME:-tg_bot-container}"
    stop_signal: SIGINT
    build:
      context: .
    network_mode: "host"
    working_dir: "/usr/src/app/${BOT_NAME:-tg_bot}"
    volumes:
    - .:/usr/src/app/${BOT_NAME:-tg_bot}
    command: celery -A tgbot.services.celery_init:celery_app worker --loglevel=INFO & python3 -m bot
    restart: always
    env_file:
      - ".env"
